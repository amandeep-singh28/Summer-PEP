****Lang Chain****
It is an open source framework which is used to develop LLM(Large Language Models) applications
- Fundamentals
- RAG
- AI Agents

For example:
There is a PDF(E Book), what it does is This is machine learning handbook(Every concept of ML is there). We want to chat with this pdf and ask some questions like:- What is linear regression, Summarize page number 20, etc.
When LLM was not there it was very difficult to make this type of application.
There are two main challenges in this- We want our model to understand what the user is asking(English -> NLU), Context awareness.
So, what we will do??

- Alternatives to LangChain
LLama Index
Hey Stack

pip install langchain
pip install langchain_openai

pip install langchain_google_genai

****LangChain: Simplifying LLM Provider Switching****
If we change the LLM provider than we need to change our code also(OpenAI, Anthropic)
For example:-
->Using OpenAI
from openai import OpenAI

client = OpenAI()

response = client.chat.completions.create(
    model="gpt-5",
    messages=[{"role": "user", "content": "Hello"}]
)
print(response.choices[0].message.content)

-> Using Anthropic
from anthropic import Anthropic

client = Anthropic()

response = client.messages.create(
    model="claude-sonnet-4",
    max_tokens=100,
    messages=[
        {"role": "user", "content": "Hello"}
    ]
)
print(response.content[0].text)

->Solution: LangChain Models
We have models parameter in our LangChain -> It makes our life easy by providing a common interface

(i)OpenAI with LangChain
from langchain_openai import ChatOpenAI

llm = ChatOpenAI(
    model="gpt-5"
)

response = llm.invoke("Hello")

print(response.content)

(ii)Anthropic with LangChain
from langchain_anthropic import ChatAnthropic

llm = ChatAnthropic(
    model="claude-sonnet-4"
)

response = llm.invoke("Hello")

print(response.content)

****Parameters of langchain****
(i)model
(ii)max_completion_token = Integer Value
It is directly proportional to length.
Two types of token:
    Input and output token
If the input token is large than the cost would also be high. Similarly for the output token as well.
Token has cost assosciative with it.
Token(I/p) < Token(O/p)

(iii)temperature
The temperature parameter controls the randomness or creativity of the model's output.
    Low temperature → More deterministic and focused answers
    High temperature → More creative and diverse answers

****Open Source Models****
Cons of frontier paid models but advantages of open source model:-
(i)Cost
(ii)Privacy
(iii)Network
(iv)Fine tuning
We can also make our open source model "paid" by making some changes in it.

-> How to use open source models
Example: LLama(developed by meta) -> Host(Model ka code dalkr chordiya) -> We have to use it, we can do it in 2 ways either downloading it and run locally into the system, or downloading it and upload it on the cloud platform create api and then use it

Hugging face -> Largest repositories of all of the models from where you can download it

If we have to choose the 2nd option -> Than Hugging face can do it automatically (HF inference API) but it has cost.

****Embedding Models****
Models are of two types:-
    Text(OS & Closed) -> Input as text and output as Text
    Embedding -> Input as text but output as vectors

Work:-
(i)Create vectors for all of the paragraphs(Documents)
(ii)Create vector for query
(iii)Find the closest document vector for the query vector
(iv)Closest would be the answer

dimensions = 500, length of each vector (Even if there is a single word, the single word will represent 500 length)


****08-07-2026****
****Chain****
What are we doing?
Pipeline: Input → Prompt → Model → Parser → Output
We first provide an input to the application.
That input is converted into a prompt (either a Single Message Prompt or a Multi-Message Prompt).
The prompt is then sent to the LLM (Model).
The model generates a response.
The response is passed to an Output Parser, which formats or extracts the required information.
Finally, the parsed result is displayed as the output.

Here are the steps in a short and easy-to-remember format:

(i)Import the required libraries.
(ii)Create the Prompt Template (ChatPromptTemplate).
(iii)Create the Model (ChatGoogleGenerativeAI).
(iv)Create the Output Parser (StrOutputParser).
(v)Invoke the Prompt using chat_prompt.invoke() to generate the final prompt.
(vi)Invoke the Model using model.invoke() to get the model's response (AIMessage).
(vii)Invoke the Parser using parser.invoke() to extract the text from the model's response.
(viii)Print the final output.

Flow Diagram
User Input
     ↓
ChatPromptTemplate
     ↓
chat_prompt.invoke()
     ↓
PromptValue
     ↓
model.invoke()
     ↓
AIMessage
     ↓
parser.invoke()
     ↓
String Output
     ↓
print()
One-line summary for exams

Import Libraries → Create Prompt → Create Model → Create Parser → Invoke Prompt → Invoke Model → Invoke Parser → Print Output.

To solve this problem, we use Chain(See example Gen_AI -> chatPromptUsingChain):
    chain = chat_prompt | model | parser

Task 1:-
(i)Create a file joke_generator.py
(ii)User_input = "topic"
(iii)no_of_jokes = int Value
(iv)Prompt:- ChatPromptTemplate, Model:- ChatOpenAI, Parse:- StrOutputParser

Task 2:-
I/P -> Prompt 1 -> Model 1 -> Parser -> Prompt 2(Analyze this joke and Create a better joke than this {response} Return the old joke and new joke with analyzes) -> Model 2 -> Parser -> Output

****Use of parallel chains****
A Parallel Chain executes multiple chains at the same time using the same input (or different mapped inputs), instead of waiting for one chain to finish before starting the next.

Sequential Chain (what you've done)
Input
  │
  ▼
Prompt 1
  │
  ▼
Model 1
  │
  ▼
Parser
  │
  ▼
Prompt 2
  │
  ▼
Model 2
  │
  ▼
Parser
  │
  ▼
Output

Here, Prompt 2 cannot start until Prompt 1 finishes because it needs Prompt 1's output.

Parallel Chain
                    Input
                      │
      ┌───────────────┴───────────────┐
      ▼                               ▼
  Prompt 1                        Prompt 2
      │                               │
      ▼                               ▼
   Model 1                        Model 2
      │                               │
      ▼                               ▼
    Parser                         Parser
      │                               │
      └───────────────┬───────────────┘
                      ▼
                 Combined Output

           Input
              │
      ┌───────┼────────┐
      ▼       ▼        ▼
   Prompt1 Prompt2 Prompt3
      │       │        │
      ▼       ▼        ▼
    Model   Model    Model
      │       │        │
      ▼       ▼        ▼
    Parser  Parser   Parser
      │       │        │
      └───────┼────────┘
              ▼
           Output

Both chains must start at the same time.

Task:- User will give a city
Chain1:- Top Tourist attraction
Chain2:- Food places
Chain3:- Best time to vist

                     Input (City)
                          |
                          |
            ---------------------------------
            |               |               |
            |               |               |
     Tourist           Food           Best Time
      Prompt           Prompt           Prompt
            |               |               |
          Model           Model           Model
            |               |               |
          Parser          Parser          Parser
            \               |              /
             \              |             /
              \_____________|____________/
                        |
                        |
                RunnableParallel
                        |
                        |
                 Combined Output

We can create graph of chains also (Using Lang graph)
Lang spin observe models how it is performing

****Conditional Chaining****
RunnableBranch is used to implement conditional chaining in LangChain. It allows you to execute different chains based on a condition.

from langchain_core.runnables import RunnableBranch

conditional_chain = RunnableBranch(
    (func1, chain1),
    (func2, chain2),
    default_chain
)
How it works
Each function (func1, func2, ...) acts as a condition and must return either True or False.
RunnableBranch evaluates the conditions from top to bottom.
As soon as a condition returns True, the corresponding chain is executed.
The remaining conditions are not checked.
If none of the conditions return True, the default_chain is executed (if provided).
Example Flow
            Input
              |
              |
     -------------------
     |                 |
   func1?           False
     |
   True
     |
   chain1
     |
    Output

If func1 = False
        |
      func2?
     /     \
  True     False
   |          |
chain2   default_chain
   |          |
 Output     Output
Key Points
RunnableBranch works like an if → elif → else statement in Python.
Conditions are evaluated sequentially.
The first matching condition determines which chain is executed.
A default_chain is optional but recommended to handle cases where no condition matches.

Task:-
Create a project Story Generator
Based on the age, you will be geenrating stories
  if (age < 18), create kids stories
  elif (age > 18) create horror stories

****09-07-2026****
Three components are already convered, Three are left
Indexes, Memory and AI Agents

-> RAG(Part of index component)
What we were doing till now?
I/p(Prompt) -> LLM -> O/P(Response)
But there are some problems in this:-
    (i)LLMs model are based on parameter, weights and biases. All of the information are stored in the parameters. If we change this the behaviour of the model will be changed. There are some issues with this set up, for example:- If we want some information related to private / company data. If we ask something about summarize my email box, it will not do that because emails are not publicly available
    We have a company Krishna, there are some rules and regulations in this company. This data is not publicly available on the internet, "How we are doing in the last quarter", It will not able to do that
    (ii)These LLMs model has cutoff date, If we train it today, the cutoff date would be today itself. If we want to ask something beyond the cutoff date, it won't be able to answer.
    (iii)Hallucinations (Airline case)
    when an AI generates confident, plausible-sounding, but entirely false or nonsensical information
    (iv)Private information (Privacy)

-> Solution
(i) Fine tuning (RLHF, LORA) {Only big companies do these because computational expensive, It is not that easy to do fine tuning}
private/ company data is solved, cutoff date is also solved(Again fine tuned the data -> But expensive), Hallucinations (upto particular extent we can solve this, give examples, if not then directly say no)
Want to use that model on a particular way.
Take any open source model which is already trained, try to retrain again with the specific data.
We will play with the parameters like temperature, weight and biases.

Company "Outlier"
They fine tuned these models. We gave wrong prompts and than retrained the model using RLHF

(ii)Context Engineering
We gave prompts and on the basis of that we trained the models by giving examples.

****RAG -> Extra knowledge + Prompt*****
Along with the prompt we gave extra knowledge. This extra knowledge can serve as extra information which was not there previously with the trained model.
Prompt -----
            ---> Improved Prompt -> LLM -> Response
Context ----

-> Steps in RAG

(i)Indexing
We load all the data, whatever data we want RAG to act upon.
We load everything to the separate storage

(ii)Retrieve
Then we retrieve the relevant data from the Index. 

(iii)Augmentation
Combined the retrieved information + Prompt

(iv)Response
Then we are getting the response

****Indexing****
(i)Document Loading
Load data from the different sources.
For example:-
  PDF
  Word document
  Text file
  Website
  Database
  CSV

(ii)Text Splitting (Chunking)
Large document gets splitted into chunks

(iii)Embedding of the chunks
Vectorization

(iv)Store in Vector Database (Indexing)
The generated vectors, along with their corresponding text chunks and metadata, are stored in a vector database.

-> How to perform Indexing??
(i)Use the DocumentLoader class from the langchain. This would be responsible for loading the data.
  If working with pdf -> Then PDFLoader
  If working with text -> TextLoader
Similarly we have loaders for everything.
We can create custom DocLoader also
Syntax of all the loaders is same

  from langchain_community.document_loaders import PyPDFLoader
  loader = PyPDFLoader('D:\Summer PEP\Smart_IT_Support_Ticket_Classifier_Project_Plan_Updated.pdf')
  documents = loader.load()
  print(documents[0].page_content)

If we have to work with multiple files, than we can use DirectoryLoader. We have to store all of the files in that Directory. It will start loading recursively.
  Create a folder, and store 2-3 pdfs and use this syntax:-
    from langchain_community.document_loaders import DirectoryLoader
    from langchain_community.document_loaders import PyPDFLoader

    loader = DirectoryLoader("Data Folder/", glob="**/*.pdf", loader_cls=PyPDFLoader, show_progress=True)
    documents = loader.load()

    print(f"Number of Documents: {len(documents)}")

    for idx, document in enumerate(documents, start=1):
        print(f"\nDocument {idx}")
        print("Content:\n", document.page_content)
    print("Metadata:\n", document.metadata)

-> Mini Project - LPU RAG 
Fetch all info of LPU from official website which is publicly available
go to the webpage , parse html structure , find all the links available, keep pushing it to the stack 

While stack:
    check_if_official(link)

Check the link whether the selected link is of my use or not 
load all the info from the valid link using webpage loader 
We only want to scan 500 pages - apply this constraints . 


                  User Starts Project
                         │
                         ▼
         Start with Official LPU Homepage
        https://www.lpu.in/
                         │
                         ▼
            Download HTML of Homepage
                         │
                         ▼
             Parse HTML using BeautifulSoup
                         │
                         ▼
            Extract all hyperlinks (<a>)
                         │
                         ▼
          Push every discovered link into Stack
                         │
                         ▼
               While Stack is NOT Empty
                         │
                         ▼
              Pop one URL from Stack
                         │
                         ▼
             Is URL already visited?
                 │              │
               Yes             No
                │               │
                ▼               ▼
             Ignore       Check official?
                                │
                    ┌───────────┴───────────┐
                    ▼                       ▼
                Official                External
                    │                       │
                    ▼                       ▼
          Load webpage using          Ignore
          WebBaseLoader
                    │
                    ▼
           Save page content
                    │
                    ▼
      Extract more links from page
                    │
                    ▼
      Push new official links to Stack
                    │
                    ▼
       Stop after visiting 500 pages
                    │
                    ▼
          Documents Ready for RAG

(ii) Chunking
We try to split the documents into several chunks (sub documents).
Advantages:
  - Performance
  - To optimize cost and resources
  - Context Window limit

https://www.chunkviz.com/

Different ways to perform chunking:-
  (i)Character Split Chunking
    - We have to give chunk size in this.
    - But how we will define chunk size? -> We will just try and test
    - But this is not good, because it divides a word also {Check in the website}
    - We use Chunk Overlap (Current chunk trying to save the previous chunk data also)
  (ii)Recursive Character Chunking(Best)
      First it divide the whole text into paragraph(\n\n) but if it is not possible to divide the paragraph, it will divide into sentences, if not possible than into words

  (iii)Document structure chunking
      Document gets splited based on its logical structure.
      For example:-
        If we have to do the chunking of a python code. Than we cannot split the document into "\n\n"(para) or "\n"(line), we can do the splitting like whenever a function end do the split, or a variable is declared.
  (iv)Semantic Chunking
      We try to find the semantic meaning of the sentences and based on that semantic meaning we try to club them.
      For example:-
        s1 - s2 - s3 - s4 ---- sn (Vectors)
        If s1 and s2 are similar, than we combine them. So it become (s1 & s2)
        Now if s3 is also same than it becomes (s1 & s2 & s3)
        If s4 is different than (s1 & s2 & s3, s4 --)
  (v)Agentic chunking

-> Vector Database (Traditional databases are designed for exact matching, whereas vector databases are designed for similarity search.)
It is used to store data as vectors
(i)Store Vectors
(ii)Similarity search
(iii)Indexing
(iv)CRUD operations

How indexing works:
      V1
      V2
      V3      We create clusters, for example:- 10 clusters are made and approx 1000 vectors
Vq    |       are there in each cluster and a new query vector comes in so we check the cluster
      |       which are same as of the query vector. Than we search in that cluster
      |
      |
      V10,000
Vq(Query Vector)

Vector store (4 functionalities - that we discussed just above)
Vector database (4 functionalities + ACID properties + Concurrenct)
https://medium.com/@abhinavjyoti09/vector-stores-in-langchain-f2a639522f03

Text in vector out (Embedding Models)
Text in Text out (Text Model)

-> Retrievers
WData source -> Relevant documents 
Therefore we query to find the relevant information

https://medium.com/@abhishekjainindore24/langchain-part-12-retrievers-b435ae8f4ea7

Earlier, we stored documents in a vector database and retrieved them using similarity search. However, not all data needs to be stored in a vector database. A retriever can also fetch relevant documents directly from external sources like Wikipedia or Arxiv at query time. That's why LangChain provides different types of retrievers depending on where the data is stored.

Top three retrievers:-
  (i)MMR
  (ii)MQR
  (iii)CCR

-> MMR (Maximum Marginal Relevance)
Problem: Normal similarity search may return duplicate or very similar documents.
Example:
Query: Causes of Climate Change

Doc1 → Arctic glaciers are melting.
Doc2 → Ice caps are melting.
Doc3 → Greenhouse gases increase global warming.

A normal retriever might return:
Doc1 ✅
Doc2 ✅ (almost the same as Doc1)
Doc3

MMR returns:
Doc1 ✅
Doc3 ✅
Another different but relevant document

Goal: Return relevant + diverse documents.

-> MQR (Multi-Query Retriever)
Problem: A user may ask an ambiguous question.

Example:
Query:
How can I stay healthy?

The LLM rewrites it into multiple queries:

How to eat healthy?
How to exercise regularly?
How to improve mental health?
How to sleep better?

Each query searches the vector database, and all results are merged.

Goal: Improve retrieval by searching from multiple perspectives.

-> CCR (Contextual Compression Retriever)
Problem: Retrieved documents often contain unnecessary information.
Example:

Retrieved Document:
The Grand Canyon is famous.
Photosynthesis converts sunlight into energy.
Millions of tourists visit every year.

The user asked:
What is photosynthesis?

CCR compresses the document to:
Photosynthesis converts sunlight into energy.

Goal: Remove irrelevant text and keep only what's needed.

****Memory(5th component)****
-> Conversation Memory
(i)Conversation Buffer Memory
The LLM remembers the entire conversation.
Human :- x
AI:- Alpha
Human :- y + (x + Alpha)
AI:- Beta
Human :- z + (y + Beta + x + Alpha)

Disadvantage: Context Limit, Hallucination.

(ii)Conversation Summary Memory
Stores previous message as summary
Human: x
AI: Alpha
Human: y + Summary(x + Alpha)
AI: Beta
Human: z + Summary(x + Alpha + y + Beta)

(iii)Conversation Buffer Memory
It stores only the last K conversations and remove others.

(iv)Conversation Summary Buffer Memory
For the last n message, we will store as it is, beyond that we will store the summary.

-> Entity Memory
Instead of remembering the whole conversation or a summary, it remembers important entities (facts) mentioned during the conversation.
An entity can be:
  Person
  Place
  Organization
  Product
  Date
  Preference
  Relationship

-> KGM (Knowledge Graph Memory)
Knowledge Graph Memory (KGM) stores information as entities and their relationships in a graph structure. Instead of remembering only facts, it remembers how those facts are connected, enabling the LLM to perform better reasoning and provide more personalized, context-aware responses.

Jatin is husband of Vanshika
Satish is son of Jatin
Selina is sister of Satish

Jatin ->(Spouse) Vanshika
|
|
Son
|
Satish ->(Siblings) Selina

-> Vector Store Memory
Vector Store Memory stores previous conversations as embeddings in a vector database. When a new query arrives, it performs semantic similarity search to retrieve only the most relevant past conversations, allowing the LLM to remember long-term context efficiently without sending the entire chat history.

****AI Agents****
LLM + Tools

              ------Tool----Action 1
              |
              |
Goal -> LLM-- ------Tool----Action 2
              |
              |
              ------Tool----Action 3

Tools:-
  We use multiple APIs(Tools)

How to work with tools, What tools are and how to define it??
There are 4 steps:-
  (i)Create Tools
  (ii)Bind these tools with LLMs (You have access to these many tools for the reference)
  (iii)Call these tools (LLMs will use its reasoning capabilties keeping the goal in mind, which tools it want to use)
  (iv)Tools Execution (Once the LLM has figured out which tool it want to use, We will use that tool)

There are two types of tools:-
For what things we can create tool:-
  You want to access Gmail messages
  Send Gmail messages
  Multiply 2 numbers
  We want to execute python code
(i)Built-in Tools(LangChain already has)
(ii)Custom Tools
  -@ tool
  -Structured class
  -Base class(Forgot the name)


-> How to work with Inbuilt Tools
(i)Tavily search
Firstly we have to create an API key from the Travily website.
Add the api_key in the .env folder like this :- TAVILY_API_KEY = "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"

If we define multiple tools, than it will suggest only the required tools to use.

If we change the prompt. For example:- What is the capital of India?
  LLM will first check whether the information is present with it or not. If not present than it will tell us to use the tools to get that information.

-> How to work with Custom Tools
We will cover only this method (@tool)
It is just a function, define a function
  Tool - Which can add two numbers(Suppose LLM doesn't know -> We want to create this tool)?
I/p -> Two number
O/p -> Addition of two number
function(num1, num2):
  return num1 + num2

Now we want to convert this functon into a tool:-
  Three steps to convert any function into a tool:-
    (i)@ tool decorator
    (ii)Provide the type of variable
    (iii)Description

Add @tool on the top of your function:-
  @tool
  function(num1, num2):
    return num1 + num2
Declare the type of variable:-
  @tool
  function(int num1, int num2):
    return num1 + num2
Add Description(So that LLM can know what this function is doing)
  @tool
  function(int num1, int num2):
    """This function adds two number"""
    return num1 + num2

If we want to use two tools than we can write the if-else condition.
But we cannot write long-long conditions, So to solve this problem AgentClass came into the picture
  AgentClass(From LangChain)
Check agentClass.ipynb

-> Project
Make a tool that would open VsCode & Chrome on the basis of the user input

****Revision****
-> Embedding Models
Two types:-
  Embed_documents
    embed_documents() is used to convert documents or chunks into embeddings before storing them in a vector database.
  Embed_query
    embed_query() is used to convert the user's question into an embedding during retrieval.

4 types
  Static Prompt
  Prompt Template -> Whenever we want to use dynamic prompt
    PromptTemplate(
      template = """ """
      input_variables = []
    )

Messages:-
  Different roles of replies, there are four kind of message:-
    System -> You are a College assistant
    Human -> The messages which come from the humans
    AI -> Messages from AI
    Tool message -> What the tool respond to us

AIMessage (
  content = " "
)

ChatPromptTemplate:-
  Combining Prompt Template and Message into one class.
  Here we are passing dynamic prompt also and along with messages like AI, HUMAN
ChatPromptTemplate (
  ('System', '<content>'),
  ('AI', '<content>'),
  ('Human', '<content>')
)

Message placeholder:-
  We can use this in the history of all the messages to reference back previous messages.
  User has the conversation yesterday but he closed the chat and now he want to bring back those message, we can do this via Message placeholder.